[build]
builder = "NIXPACKS"
buildCommand = "cd backend && cargo build --release"

[deploy]
startCommand = "./backend/target/release/backend"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
